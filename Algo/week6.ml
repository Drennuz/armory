(* hash table
    > maintain evolving key-value store; good for massive lookups
    > insert | delete | lookup; O(1); no worst-case guarantees
    > no ordering
    > application: compiler symbol look-up; routers blacklist ip
*)

(* 2-sum problem 
    > sort first; nlogn
    > hash : linear
*)

module Hash = struct

end
